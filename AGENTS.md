# 문제 데이터 하네스

`questions/written/*.json`을 추가·변형할 때 다음을 지킨다.

- 파일별 100문항, 회차·과목·메뉴별 문항 수를 맞추지 않는다. 앱이 전체 풀에서 필요한 수만큼 뽑는다.
- 목표 수량을 먼저 정하지 않는다. 각 문항을 독립적으로 검토해 정답이 하나로 검증되는 문제만 등록한다.
- 변형 문제는 원문과 다른 방향으로 묻고 `variantOf`에 원문 `key`를 기록한다.
- 완료 조건은 수량이 아니라 `npm run check` 통과다.

`questions/memo/*.json`(암기 카드)은 규칙이 다르다.

- 전부 `type: "short"`. 4지선다로 만들면 소거법으로 맞아 회상 훈련이 안 된다.
- 표는 셀 단위로 쪼갠다. "AES의 라운드 수"가 아니라 "AES-192의 라운드 수" 한 장씩.
- 정답이 하나로 떨어지는 사실만 카드로 만든다. 답이 문장이면 기출 `ox`가 더 맞다.
- 채점은 `normalizeShortAnswer`가 공백·대소문자·`·.()_-/`를 무시한다. 그래도 표기가 갈릴 답은
  body에 형식을 못박는다 — `(파일명만)`, `(경로 전체)`, `(Feistel / SPN)`.
- 법규 숫자는 개정이 잦다. 조문을 확인하지 않았으면 쓰지 않는다. 틀린 카드는 간격 반복이 더 단단히 외우게 한다.
  확인한 근거는 `note`에 조문·시행일로 남긴다(화면에는 안 나온다. 다음에 재검증할 사람을 위한 것).
