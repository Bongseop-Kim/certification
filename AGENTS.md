# 문제 데이터 하네스

`questions/written/*.json`을 추가·변형할 때 다음을 지킨다.

- 파일별 100문항, 회차·과목·메뉴별 문항 수를 맞추지 않는다. 앱이 전체 풀에서 필요한 수만큼 뽑는다.
- 목표 수량을 먼저 정하지 않는다. 각 문항을 독립적으로 검토해 정답이 하나로 검증되는 문제만 등록한다.
- 변형 문제는 원문과 다른 방향으로 묻고 `variantOf`에 원문 `key`를 기록한다.
- 완료 조건은 수량이 아니라 `npm run check` 통과다.

`type: "short"`(단답) 문항은 규칙이 더 있다. 원본은 `scripts/short/cards_<subject>.py`이고
`python3 scripts/short/build.py <subject>`가 `questions/written/short-<subject>.json`을 만든다. JSON을 직접 고치지 않는다.
설계 근거와 후보 목록은 `plans/short-answer-rebuild.md`.

- 방향은 설명→용어. 정답은 시험 보기에 나올 형태(용어·약어·숫자)다. "~의 정의는?"처럼 답이 문장이 되면 단답이 아니다.
- 한 카드 한 값. "SEED의 블록/키/라운드"는 세 장으로 쪼갠다. 열거("4가지는?")는 원소 하나만 묻는다.
- 표형 지식(포트, 알고리즘 파라미터, 로그 파일)은 기출에 나온 행에 대해 셀마다 한 장. 표 전체를 만들지 않는다.
- 약어가 있으면 약어가 정답(OCSP, NAC, RBAC). 한/영 표기가 갈리는 답은 body 끝에 형식을 못박는다 —
  `(영문 약어)`, `(한글)`, `(숫자만)`, `(경로 전체)`, `(Feistel / SPN)`, `('___ 공격'의 빈칸)`.
- 채점은 `normalizeShortAnswer`가 공백·대소문자·`·.()_-/`를 무시한다. 그래서 정답에 괄호·설명·조사를 넣지 않고,
  `-`나 `.`만으로 된 답은 만들지 않는다(정규화하면 빈 문자열이다).
- 노트(`scripts/notes`)에 있다는 이유로 만들지 않는다. 기출 정답 보기, 복수 회차 반복, 출제기준 항목 중 하나에 걸려야 한다.
  근거는 `note`에 적는다(`기출 2023-03-11#48`, `notes IMG_5220`, `개인정보보호법 §34③ 2023.9.15`). 없으면 `check`가 막는다.
- 법규 숫자는 개정이 잦다. 조문을 확인하지 않았으면 쓰지 않고, 기관 목록처럼 자주 바뀌는 것은 만들지 않는다.
